import Cocoa

// Exercise 1.32
// a) Show that sum and product are both special cases of a still more general notion called accumulate that combines a collection of terms, using some general function

// func accumulate(combiner: